# 웹 체험판 Vercel 스파이크 결과

- 상태: **로컬 기준값만 측정함. Vercel 측정은 배포 후 채운다**
- 계획: `docs/superpowers/plans/2026-09-15-web-trial-plan1-parser-and-spike.md` Task 6

## 1. 로컬 기준값 (2026-09-15)

- 장비: Apple M4, Python 3.14, 워크트리 venv
- 경로: `problem_parser.parse_problems()` — OCR·AI 끔, 칠판 cutout 없음
- 입력: 실제 텍스트 PDF의 앞 3쪽(`fitz` `select`로 자름). 파일은 저장소에 넣지 않았다
- 크기 주의: 아래 표의 "3쪽 파일 크기"는 `select` 뒤 `garbage` 없이 저장해 **원본에서 안 쓰는 객체가 남은 값**이다. 실제 크기는 1-1 표를 본다

| 입력 | 3쪽 파일 크기(정리 전) | 전체 | 인식 | crop | 최대 RSS | 결과 |
|---|---|---|---|---|---|---|
| 2026학년도 수능 국어(언어와매체) 홀수형 | 2.89 MB | 1.70초 | 0.99초 | 0.55초 | 556 MB | 문항 1~9 + 지문 3개, 확인 필요 7개 |
| 2026학년도 9월 모평 국어(언어와매체) | 3.88 MB | 1.63초 | 0.95초 | 0.53초 | 556 MB | 문항 1~9 + 지문 2개, 확인 필요 7개 |

### 1-1. 파일 크기 (정정, 2026-09-15)

처음 기록에서 "3쪽만 잘라도 2.9~3.9 MB라 4 MB 상한에 가깝다"고 판단했으나 측정 실수였다. PyMuPDF `select()` 뒤
`save()`에 `garbage`를 주지 않으면 지운 쪽이 쓰던 글꼴·이미지 객체가 파일에 그대로 남는다. 다시 잰 값:

| 입력 | 원본 전체 | 앞 3쪽 (정리 없이 저장) | 앞 3쪽 (`garbage=4, deflate`) | 앞 3쪽 (+ 글꼴 subset) |
|---|---|---|---|---|
| 2025학년도 수능 국어(언어와매체) 홀수형 | **3.71 MB / 16쪽** | 3.70 MB | 0.74 MB | 0.53 MB |
| 2026학년도 9월 모평 국어(언어와매체) | **3.71 MB / 16쪽** | 3.70 MB | 0.43 MB | 0.24 MB |
| 2026학년도 수능 국어(언어와매체) 홀수형 | **2.76 MB / 16쪽** | 2.76 MB | 0.40 MB | 0.21 MB |

### 이 값에서 읽을 것

- **시간·메모리는 여유가 있다.** 스펙 기준(웜 p95 20초, RSS 1.6 GB)의 1/10, 1/3 수준이다. Vercel 1 vCPU가 M4 코어보다 2~3배 느려도 5초 안쪽이다.
- **원본 16쪽 국어 시험지도 4 MB 안에 들어간다.** 텍스트 PDF에서는 Vercel 본문 상한이 흔한 걸림돌이 아니다. 그림이 많은 과목(과학·사회)은 따로 확인해야 한다.
- **사용자는 원본 전체(16쪽)를 올린다.** 스펙대로 "3쪽 초과 = 거절"이면 실제 시험지 대부분이 거절된다. "앞 3쪽만 체험"으로 바꿀지 Plan 2 전에 정한다. 서버가 자를 때는 반드시 `garbage=4`로 저장한다.
- "확인 필요" 표시가 12문항 중 7개로 많다. 결과 화면이 경고로 뒤덮이지 않게 Plan 2에서 표시 기준을 정한다.

## 2. Vercel 측정

- 배포 커밋:
- 함수 설정: icn1, 2 GB, maxDuration 60초, Python
- 번들 크기:

| 입력 | 쪽수 | 크기 | 콜드 wall | 콜드 import | 웜 wall p50 | 웜 parse p50 | 최대 RSS | 문항 수 |
|---|---|---|---|---|---|---|---|---|
| 합성 2문항 | 1 | | | | | | | |
| 2026 수능 국어 원본 | 16→3 | 2.76 MB | | | | | | |
| 2026 9월 모평 국어 원본 | 16→3 | 3.71 MB | | | | | | |

## 3. 판단

- 스펙 기준: 웜 p95 20초 이하, 최대 RSS 1.6 GB 이하, 번들 500 MB 이하
- 결과:
- 다음:
